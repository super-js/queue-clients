# @super-js/queue-clients
Wrapper to unify interface across multiple different messaging protocols to act as a client. Written in Typescript for NodeJS.

Currently, only basic MQTT functionality implemented